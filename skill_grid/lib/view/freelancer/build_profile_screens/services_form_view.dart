import 'package:flutter/material.dart';

class ServicesFormView extends StatelessWidget {
  const ServicesFormView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      child: Center(
        child: Text("Services form"),
      ),
    );
  }
}