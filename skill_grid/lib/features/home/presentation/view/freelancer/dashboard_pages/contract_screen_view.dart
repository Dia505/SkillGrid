import 'package:flutter/material.dart';

class ContractScreenView extends StatelessWidget {
  const ContractScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: 
        Text("Contract Screen"),
      ),
    );
  }
}